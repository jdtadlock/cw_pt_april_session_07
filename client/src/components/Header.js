import React from 'react';
import './Header.css';

const Header = props => (
  <header className="row split">
    <h3>NoteWiz</h3>

    <nav>
      <span>Login</span>
    </nav>
  </header>
);

export default Header;